{
    "name": "igniteui-for-angular-docfx",
    "version": "1.0.0",
    "description": "Static sample site generator for Ignite UI for Angular.",
    "main": "index.js",
    "scripts": {
        "start": "gulp serve",
        "build": "gulp build",
        "build-staging": "set NODE_ENV=staging && gulp build",
        "build-production": "set NODE_ENV=production && gulp build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Infragistics",
    "license": "ISC",
    "dependencies": {
        "browser-sync": "^2.24.6",
        "del": "^3.0.0",
        "dotenv": "^4.0.0",
        "gulp": "^4.0.0",
        "gulp-autoprefixer": "^5.0.0",
        "gulp-sass": "^4.0.1",
        "gulp-shell": "^0.6.5",
        "igniteui-docfx-template": "1.0.14",
        "yargs": "^10.0.3"
    }
}
