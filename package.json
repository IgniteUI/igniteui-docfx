{
  "name": "igniteui-for-angular-docfx",
  "version": "1.0.0",
  "description": "Static sample site generator for Ignite UI for Angular.",
  "main": "index.js",
  "scripts": {
    "start": "gulp serve",
    "build": "gulp build",
    "build-gh-actions": "gulp buildTravis",
    "build-staging": "set NODE_ENV=staging && gulp build",
    "build-production": "set NODE_ENV=production && gulp build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Infragistics",
  "license": "ISC",
  "dependencies": {
    "igniteui-docfx-template": "^2.9.2",
    "natives": "^1.1.6",
    "sass": "^1.30.0",
    "yargs": "^10.0.3"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "del": "^5.1.0",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^7.0.1",
    "gulp-dart-sass": "^1.0.2",
    "gulp-file-include": "^2.1.1",
    "gulp-replace": "^1.0.0",
    "gulp-shell": "^0.7.1",
    "slash": "^3.0.0"
  }
}
